import { LoginForm } from "@/components/auth/LoginForm";

/**
 * LoginPage
 * Página de acceso que renderiza el formulario de inicio de sesión.
 */
export function LoginPage() {
  return <LoginForm />;
}
